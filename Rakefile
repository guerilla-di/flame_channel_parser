# -*- ruby -*-

require 'rubygems'
require 'hoe'

Hoe.spec 'flame_channel_parser' do
  # developer('FIX', 'FIX@example.com')

  # self.rubyforge_name = 'flame_channel_parserx' # if different than 'flame_channel_parser'
end

# vim: syntax=ruby
